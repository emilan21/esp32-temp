services:
  room-sensor-server:
    build:
      context: ./server
    environment:
      PORT: ${SERVER_PORT:-8080}
    ports:
      - "${SERVER_PORT:-8080}:${SERVER_PORT:-8080}"
    volumes:
      - ./server/data:/data
    restart: unless-stopped
